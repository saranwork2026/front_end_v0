# Allow esbuild (vite's bundler) to run its postinstall build script. Without
# this, pnpm blocks the script and `pnpm install` fails with
# ERR_PNPM_IGNORED_BUILDS in CI. Read by pnpm across versions (v9 on the
# Jenkins agent, v11 locally).
only-built-dependencies[]=esbuild
